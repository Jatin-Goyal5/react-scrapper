CREATE TABLE blogs (
    id SERIAL PRIMARY KEY,
    tag VARCHAR,
    creator VARCHAR,
    title VARCHAR,
    details Varchar,
    link VARCHAR,
    preview VARCHAR, 
    relatedTags Varchar,
    post Varchar
);